import { BearFileDropZoneDirective } from './bear-file-drop-zone.directive';

describe('BearFileDropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new BearFileDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
